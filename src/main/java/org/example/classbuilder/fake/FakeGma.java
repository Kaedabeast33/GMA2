package org.example.classbuilder.fake;

import org.example.gma.templates.GMATemplate;

public class FakeGma extends GMATemplate {

    public FakeGma(String name, String description, String[] tags, String gmaId, String dbType, String dialect) {
        super(name, description, tags, gmaId, dbType, dialect);
    }
}
